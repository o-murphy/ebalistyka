name: ebalistyka_db
description: >-
  DEPRECATED — superseded by packages/ebc_db. No longer a dependency of the
  main app; kept on disk pending removal (Phase 6 of
  docs/backlogs/8.PROTOBUF_STORAGE_MIGRATION.md). See that package's
  README.md.
version: 0.0.1
homepage:
publish_to: 'none'

environment:
  sdk: ^3.11.4

dependencies:
  objectbox: ^5.3.2
  objectbox_flutter_libs: ^5.3.2
  json_annotation: ^4.11.0
  archive: ^4.0.2

dev_dependencies:
  build_runner: ^2.13.1
  objectbox_generator: ^5.3.2
  json_serializable: ^6.8.0
  test: ^1.31.0
