# Open question — Weapon/Sight/Ammo `image` field format

**Status:** Proposed

Format and storage for `entity.image` (Weapon/Sight/Ammo) is still TBD —
candidates are filesystem path, base64, or bundled asset reference. Beta
scope; blocks the image picker/camera UI in wizard screens (tracked under
[0001]).
